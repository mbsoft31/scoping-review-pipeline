"""Web package for the Systematic Review Pipeline.

This package contains the FastAPI application and supporting modules for
the optional web dashboard.  The dashboard allows users to interact
with the pipeline via a modern web interface built with HTMX, Alpine.js
and Tailwind CSS.

To start the web server from the CLI use:
    srp serve --port 8000 --reload
"""
